# Repository Guide for Future Sessions

## 项目定位

这是一个 **Agent Skill 仓库**，存放 `exam-paper-error-analysis` 技能的全套定义文件。

技能用途：全学科错题分析（基础教育+职业教育），提供单题深度分析、整卷失分分析、变式题生成、错题分类讲评四大能力。

## 目录结构

```
skills/exam-paper-error-analysis/
├── SKILL.md                    # 主技能文件（触发规则、输入校验、分析原则、子能力路由）
└── references/
    ├── error-types.md          # 六类错误定义（MANDATORY 条件加载）
    ├── html-template.md        # HTML报告结构定义（MANDATORY 条件加载）
    ├── subject-adaptation.md   # 职业教育8大专业适配（职教时 MANDATORY）
    └── vocational-standards.md # 岗位能力模型（职教时 MANDATORY）
```

## 关键约束（必须遵守）

### PDF/图片处理：禁止本地 OCR

**MANDATORY**: 使用 pypdfium2 将 PDF 转为图片（scale=2），然后由 Agent 内置多模态模型识别。

```
禁止：本地 OCR 工具、外部 OCR API
正确：pypdfium2 转图片 → 多模态模型识别 → 用户确认后分析
```

标准 PDF 处理流程（pypdfium2）：`pypdfium2.PdfDocument(路径)` 打开 → 逐页（`for page in pdf`）`render(scale=2)`（保证识别质量）→ `to_pil()` → 送多模态模型识别 → `pdf.close()`。

### 参考文件条件加载

| 文件 | 加载时机 |
|------|---------|
| `error-types.md` | 子能力1/2的错误归因步骤（MANDATORY）；子能力3（推荐） |
| `html-template.md` | 任何子能力的"整合输出"步骤（MANDATORY） |
| `subject-adaptation.md` | 分析职业教育课程时（MANDATORY） |
| `vocational-standards.md` | 职教课程需岗位能力映射时（MANDATORY） |

**不预加载**：仅在对应流程步骤中按指示加载。

### HTML 输出必须包含全部 6 个章节

即使只触发一个子能力，HTML 也必须包含：
- 基本信息
- 导航目录
- 整卷失分分析（未触发时填充"本次分析未涉及此模块"）
- 错题逐题分析（未触发时填充占位）
- 变式练习（未触发时填充占位）
- 错题分类讲评（未触发时填充占位）

## 四个子能力触发关键词

| 子能力 | 关键词 |
|--------|--------|
| 单题深度分析 | 错题分析、分析这道题、这道题为什么错 |
| 整卷失分分析 | 试卷分析、失分地图、整卷分析 |
| 变式题生成 | 出变式题、生成练习、针对性练习 |
| 错题分类讲评 | 错题分类、讲评建议、怎么讲评 |

## 分析原则（NEVER 列表）

- 禁止用"粗心"直接归因（必须具体到审题偏差、计算习惯、概念不清）
- 禁止推断学生整体能力（只分析这道题的可观察表现）
- 禁止笼统建议如"多练习"（必须给出具体类型、数量、时限）
- 禁止跳过输入内容确认步骤（识别结果必须请用户确认）
- 禁止对不同错误类型输出雷同分析

## 职业教育特殊处理

当学科为机电/计算机/会计/护理/汽修/建筑/旅游/电商时：

1. **MANDATORY** 先读取 `references/subject-adaptation.md`
2. 追加第八步"职业能力发展建议"
3. 安全规程错误标注为"⚠️ 高风险"
4. 区分"理论不会做"和"操作不规范"
5. 变式题设计更换工作情境/设备型号

## 测试

端到端测试文件：`test/七年级上学期数学期末综合测试卷.pdf`

验证项：PDF→图片转换、多模态识别、七步框架、HTML结构、自我校验。

## 常见陷阱

1. **误用本地 OCR** — 正确做法是 pypdfium2 + 多模态
2. **跳过参考文件加载** — 必须按 MANDATORY 指示加载
3. **HTML 缺章节** — 未触发的章节也要保留结构占位
4. **直接给答案** — 应先诊断"为什么错"
5. **职教未做增强** — 职教课程需追加职业能力发展建议